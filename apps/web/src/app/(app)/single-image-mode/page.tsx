import { EmptyState } from "../../../components/empty-state";

export default function SingleImageModePage() {
  return (
    <EmptyState
      description="Single Image Mode is scheduled after the AI job foundation and asset workflow are ready."
      title="Coming in Milestone 8"
    />
  );
}
