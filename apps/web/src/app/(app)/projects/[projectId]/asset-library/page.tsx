import { EmptyState } from "../../../../../components/empty-state";

export default function AssetLibraryPage() {
  return <EmptyState description="Asset Library is scheduled as its own project-scoped module." title="Coming in Milestone 6" />;
}
