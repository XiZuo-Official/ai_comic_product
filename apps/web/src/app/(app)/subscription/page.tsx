import { EmptyState } from "../../../components/empty-state";

export default function SubscriptionPage() {
  return (
    <EmptyState
      description="Subscription UI is present as a foundation placeholder. Payment integration is not part of Milestone 1A."
      title="Subscription Placeholder"
    />
  );
}
